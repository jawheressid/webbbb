# Aggregated dependencies for all backend services
# Usage: pip install -r backend/requirements.txt

# Vision classifier (FastAPI) + shared ML stack
-r Modele_food/requirements.txt

# RAG chatbot (Flask + LangChain + Groq)
-r Modele_rag/requirements-all.txt

# Nutritionist chatbot (Groq client already covered via Modele_rag/base)

# Extras used by tests/utility scripts
httpx>=0.28.1
aiohttp>=3.13.2
langgraph>=1.0.4
pandas>=2.3.3
scipy>=1.16.3
scikit-learn>=1.7.2
pyyaml>=6.0.3
sqlalchemy>=2.0.44
tenacity>=9.1.2
tqdm>=4.67.1
